# Simulator

This folder holds the future MataMentorPi browser-simulator runtime.

Planned areas:

- `core/` for simulator state and environment helpers
- `shims/` for student-library-compatible simulator backends
- `courses/` for lesson-specific simulator scenes
- `scenes/` for reusable world definitions

